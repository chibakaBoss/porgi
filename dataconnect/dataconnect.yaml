specVersion: "v1"
serviceId: "porgi"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "porgi-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
